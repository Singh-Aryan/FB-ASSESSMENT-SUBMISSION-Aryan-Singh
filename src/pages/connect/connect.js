import React from "react";
import Connect from "../../Components/connect/Connect";

const Home = () => {
    return (
        <div style={{backgroundColor : "#1e4d91",
        height:'100vh',
        width: '100%'
        }}>
            <Connect />
        </div>
    );
};

export default Home;
